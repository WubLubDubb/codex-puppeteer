## 2026-03-24 `/activate` session switch command

- Goal: let the user switch the current chat's active managed session without having to send a real prompt just to move the binding.
- Implemented `activate` dispatch in `src/automation-agent.js`, reused managed-session resolution via `#requireSession`, and updated response formatting so the command returns the selected workspace context.
- `/activate -n <sessionId|listNumber>` now updates the current source binding only; the next plain-text message is routed to the activated session.
- Added parser coverage in `tests/message-parser.test.js` and runtime coverage in `tests/automation-agent.test.js`; verification is `npm test` => `81/81`.

## 2026-03-24 `/send` longer internal wait window

- Goal: reduce the remaining real Telegram cases where coding prompts returned only early commentary because the internal `/send` wait window expired too early.
- Investigation: checked real local runtime records in `.agent/runtime/tasks.json` and verified that the problematic coding tasks were commonly timing out at the old `120000ms` internal send wait window; also ran direct `codex exec --json` probes and confirmed development-style prompts still emit final answers through normal `item.completed` agent messages.
- Implementation:
  - added `defaultSendWaitTimeoutMs` with a default of `600000ms` in `src/default-config.js`;
  - updated `src/automation-agent.js` so `/send` passes the dedicated send timeout into the internal wait path instead of sharing the shorter explicit wait timeout;
  - exposed `CODEX_PUPPETEER_WAIT_TIMEOUT_MS` and `CODEX_PUPPETEER_SEND_WAIT_TIMEOUT_MS` in `src/telegram-entry.js`, `src/wecom-entry.js`, and `.env.example`;
  - added regression coverage in `tests/automation-agent.test.js`, `tests/wait-detection.test.js`, and `tests/telegram-entry.test.js`.
- Verification: `npm test` => `79/79`.
- Result: long coding prompts are now much less likely to come back with only the first commentary line in the initial Telegram reply.

## 2026-03-24 `/send` completion reliability and screen guidance

- Goal: make Telegram `/send` behave more like a true "wait until this round is done, then reply once" interaction, and always hand the user a ready-to-copy `/screen` follow-up command.
- Implementation:
  - updated `src/automation-agent.js` so `/wait` returns assistant text from the full waited output window instead of only the limited visible screen slice;
  - changed `/send` completion messaging so timeout cases are labeled as partial output instead of completed replies, and the user-facing message always appends `screen: /screen -n ... -c ...`;
  - increased default runtime headroom in `src/default-config.js` by raising buffered output to `500` lines and default wait timeout to `120000ms`;
  - refined `src/output-analysis.js` so substantive lines containing words like `running` are no longer falsely treated as UI-only status output.
- Tests:
  - extended `tests/automation-agent.test.js` to cover long assistant replies beyond the visible screen window, timeout partial-output labeling, and guaranteed `screen` guidance in the `/send` reply;
  - extended `tests/output-analysis.test.js` to verify that substantive lines such as `Planning Long running task` remain assistant output;
  - verified `npm test` passes with `78/78`.
- Delivered behavior:
  - `/send` now more reliably returns the whole waited answer, no longer silently discards meaningful lines just because they contain words like `running`, and always tells the user which `/screen` command to run next.

## 2026-03-24 `/list` numbered selection mode

- Goal: let Telegram users operate directly on a short numbered list instead of repeatedly copying long session ids or Codex conversation ids.
- Implementation:
  - updated `src/automation-agent.js` so each visible `/list` entry gets a per-chat number, with managed sessions first and visible local Codex history items after them;
  - stored the latest numbered selection map in memory per source chat and used it to resolve `/send -n <number>` for both managed and local-history items;
  - updated managed-session commands so `/screen`, `/read`, `/kill`, and `/enablePermission` accept numbered managed-session selections and reject numbered local-history selections with a guided `/send` hint.
- Tests:
  - extended `tests/automation-agent.test.js` to cover numbered `/list` output, numbered `/send`, numbered `/screen`, and rejection of local-history numbering for managed-session commands;
  - verified `npm test` passes with `75/75`.
- Delivered behavior:
  - normal Telegram flow can now be `/list` -> `/send -n 2 -m ...` or `/screen -n 1` without copying long identifiers.

## 2026-03-24 Direct `/send` compatibility

- Goal: remove `attach` from the normal user flow so `/list` results can be used directly with `/send`.
- Implementation:
  - updated `src/automation-agent.js` so `/send -n` first resolves managed `session-000x` ids, then falls back to local Codex conversation ids from the local history index;
  - added auto-session creation using the active workspace context, plus explicit `-w` fallback when no active project context is available;
  - kept `/attach` and `/attach-last` as compatibility commands instead of deleting them.
- Tests:
  - extended `tests/automation-agent.test.js` to cover direct send by Codex conversation id, reuse of an already-managed matching session, and explicit workspace fallback;
  - verified `npm test` passes with `70/70`.
- Delivered behavior:
  - normal flow is now `/list` -> `/send -n <sessionId or codexConversationId> -m <prompt>`.
- Handoff note:
  - if a user reports that a historical conversation resumed in the wrong project, the next refinement should make workspace selection stricter instead of always preferring the active session context.

## 2026-03-24 `/projects` readability pass

- Goal: make the Telegram `/projects` response readable on a phone without losing the ability to copy a usable create command.
- Implementation:
  - changed `src/automation-agent.js` so `/projects` renders `Projects under <root> (count)` sections with project names only;
  - moved configured aliases into a short `Shortcuts` block;
  - removed the extra summary prefix from the user-facing message and kept one concrete `/create` example line.
- Tests:
  - updated the `/projects` assertion block in `tests/automation-agent.test.js`;
  - verified `npm test` passes with `70/70`.
- Delivered behavior:
  - `/projects` is now much easier to scan in Telegram while still exposing one valid full-path example for `/create`.

## 2026-03-24 `/projects` shortcuts removal

- Goal: remove the leftover `Shortcuts` block because it is not useful in the normal personal Telegram flow.
- Implementation:
  - updated `src/automation-agent.js` so `/projects` no longer renders configured workspace aliases in the user-facing response;
  - kept project discovery and the `/create` example line unchanged.
- Tests:
  - updated the `/projects` assertion block in `tests/automation-agent.test.js`;
  - verification target is still `npm test`.

## 2026-03-24 `/list` readability pass

- Goal: make `/list` readable in Telegram when the host has both one active managed session and a large local Codex history set.
- Implementation:
  - updated `src/automation-agent.js` so managed sessions no longer show full project paths and attached Codex ids are compacted;
  - reformatted local history entries into `id` + `time/title` two-line rows;
  - reduced the default visible history window from `20` to `8` items and kept an omission line for the remainder.
- Tests:
  - refreshed the existing `/list` assertions in `tests/automation-agent.test.js`;
  - added a regression case that verifies the new 8-item history cap and `+N more` message;
  - verified `npm test` passes with `71/71`.

## 2026-03-24 `/list` expansion flags

- Goal: let the user expand the omitted local history items from the compact `/list` view without giving up the cleaner default layout.
- Implementation:
  - updated `src/automation-agent.js` so `/list -a` shows all local history and `/list -c <count>` shows a caller-selected number of entries;
  - added an inline hint to the omission line so Telegram users know how to reveal the hidden history items immediately.
- Tests:
  - added parser coverage for `/list -a -c 12` in `tests/message-parser.test.js`;
  - extended `tests/automation-agent.test.js` to cover `/list -c`, `/list -a`, and rejection of `/list -c 0`;
  - verified `npm test` passes with `73/73`.


## 2026-03-24 immediate screen hint and 1-hour send wait

- Goal: make long-running Telegram `/send` interactions easier to monitor without cluttering the final assistant reply.
- Implementation:
  - increased `defaultSendWaitTimeoutMs` from `600000ms` to `3600000ms` in `src/default-config.js` and `.env.example`;
  - updated `src/automation-agent.js` so `/send` emits a dedicated `command.screen_hint` notification with `screen: /screen -n <sessionId> -c <cursor>` before entering the long internal wait;
  - removed the screen hint from the final `/send` completion formatter so the final reply stays focused on assistant output.
- Tests:
  - refreshed `/send` assertions in `tests/automation-agent.test.js` to verify the separate screen-hint notification and the absence of duplicated screen guidance in the final message;
  - verified `npm test` passes with `81/81`.
- Delivered behavior:
  - users now receive a copyable `/screen` command immediately after `/send` starts, while the eventual completion reply is cleaner and the default internal wait window is long enough for more real coding tasks.

## 2026-03-24 文档重构与推送准备

- 目标：按当前代码基线重写核心文档，清理需求、设计、计划、验收中的乱码与过期描述。
- 已处理内容：
  - 重写 `docs/requirements.md`、`docs/system-design.md`、`docs/development-plan.md`、`docs/acceptance-report.md`；
  - 重写 `docs/change-log.md` 作为当前开发状态入口；
  - 同步 `README.md`、`.agent/current-state.md` 与 `.gitignore`；
  - 文档内容已对齐当前代码行为，包括 `/activate`、`/projects`、`/list` 编号模式、`/send` 即时 `/screen` 提示和 1 小时默认等待窗口。
- 验证：待在本轮文档更新后再次执行 `npm test` 并记录结果。

## 2026-03-24 提交与推送结果

- 本地提交：`f161dae` `feat(remote): refine send flow and align docs`
- 推送目标：`origin/main`
- 推送结果：连续两次执行 `git push origin main` 均失败，错误为 `Recv failure: Connection was reset`。
- 结论：本地提交已保留，远程仓库暂未更新；后续需要在网络可用时重新执行推送。
