# AGENTS.md

你是本仓库的执行型开发代理。

## 目标

- 根据用户自然语言理解当前任务。
- 自主选择最相关的规范和项目文档作为参考。
- 产出或更新项目说明、配置模板、代码、测试和 Git 结果。
- 前置条件不足时停止并明确说明阻塞原因。

## 仓库结构职责

- `AGENTS.md`：总控入口、规则索引和最小约束。
- `agent-specs/`：仓库内的开发与 Git 约束说明。
- `.env.example`：公开配置模板，供 clone 后直接填写。
- `README.md`：公开说明入口，应面向首次使用者。
- `src/`、`tests/`：代码与测试。
- `docs/`、`.agent/`：本地私有工作资料目录，公开仓库默认不依赖它们存在。

## 规范索引

- `agent-specs/init.md`：项目初始化、基础目录、基础文档和 Git 建仓约束。
- `agent-specs/development.md`：代码开发、测试补充、实现偏差记录和开发日志规则。
- `agent-specs/testing.md`：测试验收、测试证据、验收报告和测试回写规则。
- `agent-specs/git.md`：提交、推送和 Git 状态记录规则。

## 通用规则

- 先理解用户意图，再自主选择最相关的 1 个或多个规则文件。
- 当前目录默认视为独立项目根目录，不继承父仓库管理。
- 所有规范默认从 `agent-specs/` 读取。
- 规则用于说明文档该写什么、何时更新、如何同步，不用于限制用户必须使用固定命令短语。
- 公开仓库默认以 `README.md`、`.env.example`、源码和测试为主要事实来源，不应假设 `docs/` 或 `.agent/` 一定存在。
- 若本地存在 `docs/` 或 `.agent/`，可以把它们当作补充参考，但不能把它们当成公开交付所必需的输入。
- 不要重复创建语义冲突的文档或配置模板。
- 若改动会影响公开使用方式，必须同步检查 `README.md`、`.env.example`、相关测试是否需要更新。

## 失败处理

遇到以下情况必须停止并报告：

- 无法判断用户当前意图。
- 公开仓库所需的关键信息缺失或冲突。
- Git 远程、认证或权限不足。
- 需求或实现存在互斥约束，无法安全实现。

报告至少包含：

1. 原因
2. 影响阶段
3. 缺失或冲突的输入
4. 建议下一步

## 优先级

1. 用户当前明确指令
2. `AGENTS.md`
3. 与当前任务最相关的 `agent-specs/*.md`
4. `README.md`、`.env.example`
5. 代码、测试和实际运行结果

