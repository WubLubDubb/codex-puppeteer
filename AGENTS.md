# AGENTS.md

你是本仓库的执行型开发代理。

## 目标

- 根据用户自然语言理解当前任务。
- 自主选择最相关的规范和项目文档作为参考。
- 产出或更新项目文档、代码和 Git 结果。
- 前置条件不足时停止并明确说明阻塞原因。

## 仓库文档职责

- `AGENTS.md`：总控入口、规则索引和最小约束。
- `agent-specs/`：各类规范文档，既说明何时参考，也说明内容该怎么写。
- `docs/`：项目文档产物。
- `docs/change-log.md`：开发日志索引与当前状态入口。
- `docs/dev-logs/`：按日期归档的开发日志明细。
- `.agent/state.md`：项目阶段状态摘要。
- `.agent/current-state.md`：便于接手的当前上下文摘要。
- `.agent/traceability.md`：需求、设计、实现与验收之间的追踪关系。
- `src/`、`tests/`：代码与测试。

## 规范索引

- `agent-specs/init.md`：项目初始化、基础目录、基础文档和 Git 建仓约束。
- `agent-specs/requirements.md`：需求文档的用途、结构、写法和同步原则。
- `agent-specs/design.md`：系统设计文档的用途、结构、写法和同步原则。
- `agent-specs/planning.md`：开发计划文档的用途、结构、写法和同步原则。
- `agent-specs/development.md`：代码开发、测试补充、实现偏差记录和开发日志规则。
- `agent-specs/testing.md`：测试验收、测试证据、验收报告和测试回写规则。
- `agent-specs/git.md`：提交、推送和 Git 状态记录规则。

## 通用规则

- 先理解用户意图，再自主选择最相关的 1 个或多个规则文件。
- 当前目录默认视为独立项目根目录，不继承父仓库管理。
- 所有项目文档默认写入 `docs/`。
- 所有规范默认从 `agent-specs/` 读取。
- 所有状态和追踪信息默认写入 `.agent/`。
- 规则用于说明文档该写什么、何时更新、如何同步，不用于限制用户必须使用固定命令短语。
- 若用户要求“基于某份文档修改另一份文档”，应以目标文档对应规则为主，以上游文档和相关规则为参考。
- 不要重复创建语义冲突的文档。
- 需求变更后要检查设计、计划、实现、验收是否需要同步更新。
- 实现偏离设计时，必须记录偏差和原因。
- 当 agent 在缺乏对话上下文的情况下进入开发、测试或 Git 阶段时，必须先读取 `docs/change-log.md` 作为项目开发历史入口。
- 若仅凭 `docs/change-log.md` 仍不足以恢复上下文，应继续读取 `.agent/current-state.md`、最近 3 份 `docs/dev-logs/` 日志，以及与当前任务直接相关的需求、设计、计划或验收文档。
- `docs/change-log.md` 的职责是提供当前开发状态、最近完成内容、关键文件入口和日志索引。
- 开发日志明细默认按 `docs/dev-logs/<YYYY>/<YYYY-MM-DD>.md` 归档；同一天内的多次工作应追加到同一文件。
- `.agent/state.md` 记录项目阶段状态，`.agent/current-state.md` 记录便于后续 agent 接手的当前实现上下文、阻塞和下一步建议。

## 文档依赖

- `docs/requirements.md` 是设计、计划、开发与验收的重要上游依据。
- `docs/system-design.md` 是开发、测试和设计回写的重要依据。
- `docs/development-plan.md` 用于组织实现顺序、里程碑与交付物。
- `docs/acceptance-report.md` 反映最近一次测试验收结果，不替代需求和设计本身。
- 任一核心文档被显著修改后，都要检查其他相关文档、日志和状态摘要是否需要同步更新。
- 上游文档冲突、缺失或明显过期时，必须先报告问题，而不是假装可以安全推进。

## 失败处理

遇到以下情况必须停止并报告：

- 无法判断用户当前意图。
- 前置文档缺失或冲突。
- Git 远程、认证或权限不足。
- 需求或设计存在互斥约束，无法安全实现。

报告至少包含：

1. 原因
2. 影响阶段
3. 缺失或冲突的输入
4. 建议下一步

## 优先级

1. 用户当前明确指令
2. `AGENTS.md`
3. 与当前任务最相关的 `agent-specs/*.md`
4. `docs/` 中现有项目文档
5. 代码、测试和实际运行结果

